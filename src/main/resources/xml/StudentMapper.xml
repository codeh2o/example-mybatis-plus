<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mp.dao.StudentMapper">
    <insert id="insertStudent" >
        INSERT INTO student(name,age,email,status) values (#{name},#{age},#{email},#{status})

    </insert>
    <select id="selectStudentById" resultType="com.example.mp.entities.Student">
        select id,name,age,status from student where  id = #{id}
    </select>

    <select id="selectStudentByName" resultType="com.example.mp.entities.Student">
        select id,name,age,status from student where  name = #{name}
    </select>
</mapper>